/**
 * A class to represent a 4 dimensional vector
 * @class Vector4
 * @param {number} x
 * @param {number} y
 * @param {number} z
 * @param {number} w
 */

export default class {
  constructor(x = 0, y = 0, z = 0, w = 0){
    this.x = x;
    this.y = y;
    this.z = z;
    this.w = w;
  }
}
